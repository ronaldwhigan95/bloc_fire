name: user_repo
description: Dart package which manages the user domain.
publish_to: none

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  equatable: ^2.0.5
  uuid: ^3.0.7

dev_dependencies:
  very_good_analysis: ^4.0.0+1